import { Password.Validator } from './password.validator';

describe('Password.Validator', () => {
  it('should create an instance', () => {
    expect(new Password.Validator()).toBeTruthy();
  });
});
