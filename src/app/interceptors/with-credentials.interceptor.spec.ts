import { WithCredentials.Interceptor } from './with-credentials.interceptor';

describe('WithCredentials.Interceptor', () => {
  it('should create an instance', () => {
    expect(new WithCredentials.Interceptor()).toBeTruthy();
  });
});
